---
description: gstack ship workflow (sync, test, PR) — OpenAI-compatible
mode: subagent
---

Read and execute:

`.claude/skills/gstack/ship/SKILL.md`

Use bash/git via OpenCode permissions. Confirm risky git operations with the user in chat when the skill would use `AskUserQuestion`.
