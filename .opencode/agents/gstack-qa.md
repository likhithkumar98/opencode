---
description: gstack QA + fix workflow — OpenAI-compatible
mode: subagent
---

Read and execute:

`.claude/skills/gstack/qa/SKILL.md`

Use browse CLI from `.claude/skills/gstack/browse/dist/browse` when the skill needs a browser. Ask the user in chat instead of `AskUserQuestion`.
