# `.claude/` in opencode

- **`skills/gstack/`** — [gstack](https://github.com/likhithkumar98/gstack) (plan/review/ship/browse/qa/retro). Run `./setup` inside that directory after clone.
- **`skills/<name>`** — symlinks into `gstack/<name>` for Claude Code discovery (e.g. `browse` → `gstack/browse`).

Repo root **[CLAUDE.md](../CLAUDE.md)** documents behavior; build artifacts are gitignored under `gstack/`.
